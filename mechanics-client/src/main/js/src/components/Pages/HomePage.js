import React, { Component } from "react";

import { Button } from "rsuite";

export default class HomePage extends Component {
  render() {
    return (
      <div>
        <h1>WELCOME TO MECHANICSAPP!</h1>
        <Button>HOLA!</Button>
      </div>
    );
  }
}
